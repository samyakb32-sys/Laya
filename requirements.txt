httpx>=0.27
pyyaml>=6.0
fastapi>=0.110
uvicorn>=0.27
# The Laya classifier (pulls in torch + transformers). CPU-only is fine — no GPU needed.
# Not required for classifier: heuristic / laya-http.
laya>=0.3.11
